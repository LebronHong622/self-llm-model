# config/test.yaml - 测试环境配置
model:
  context_length: 256        # 测试用最小模型

training:
  batch_size: 4
  num_epochs: 1
  device: "cpu"
  save_interval: 10
  log_interval: 5

logging:
  level: "DEBUG"

data:
  max_seq_length: 256
