# config/test.yaml - 测试环境配置
model:
  hidden_size: 128        # 测试用最小模型
  num_layers: 2
  num_heads: 2
  max_length: 64

training:
  batch_size: 4
  num_epochs: 1
  device: "cpu"
  save_interval: 10
  log_interval: 5

logging:
  level: "DEBUG"
